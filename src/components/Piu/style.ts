import styled from "styled-components";

export const Oimeuchapa = styled.p`
    color: aqua;
    font-size: large;
`
export const Container = styled.div`
   display: flex;
    padding: 24px;
    flex-direction: column;
    align-items: flex-start;
    gap: 30px;
    align-self: stretch;
    border-radius: 16px;
    background: var(--Slate-3, #202425);
`